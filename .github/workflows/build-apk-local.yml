name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: |
          cd client
          npm install

      - name: Accept Android SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Prebuild Android project
        run: |
          cd client
          npx expo prebuild --platform android --clean

      - name: Build APK
        run: |
          cd client/android
          chmod +x gradlew
          export GRADLE_OPTS="-Xmx6g -Xms2g"
          ./gradlew assembleDebug --stacktrace

      - name: Find APK files
        run: |
          echo "Searching for APK files..."
          find client/android -name "*.apk" || echo "No APK files found"
          echo ""
          echo "Listing build outputs directory..."
          ls -la client/android/app/build/outputs/ 2>/dev/null || echo "No outputs directory"
          ls -la client/android/app/build/outputs/apk/ 2>/dev/null || echo "No apk directory"
          ls -la client/android/app/build/outputs/apk/debug/ 2>/dev/null || echo "No debug directory"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: workhours-app-debug
          path: |
            client/android/app/build/outputs/apk/debug/*.apk
            client/android/app/build/outputs/apk/**/*.apk
          retention-days: 30
          if-no-files-found: warn
